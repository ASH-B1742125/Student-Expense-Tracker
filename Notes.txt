#To print value of cell
print(cell.value)

#To Print rows of sheet
print(sheet.max_row)

#To get entire row value
for row in range(1, sheet.max_row + 1) :
    cell = sheet.cell(row,3)
    print(cell.value)

#Accessing the Rows and Colums
cell = sheet['a1']
#or
cell = sheet.cell(1,1)

import openpyxl as xl
#To Load The .xlsx since it returs obj need to store
wb = xl.load_workbook('student_expense.xlsx')
#create a sheet
sheet = wb['Sheet1']

#Access all expenses from excel
for row in range(2 , sheet.max_row + 1) :
   date = sheet.cell(row, 1).value
   item = sheet.cell(row, 2).value
   category = sheet.cell(row, 3).value
   amount = sheet.cell(row, 4).value
   
   print(date, item, category, amount)

#pie chart
PieChart → creates the pie chart
Reference → selects Excel cells for chart data
#shows the percentage
bar.dataLabels = DataLabelList()
bar.dataLabels.showPercent = True
bar.dataLabels.showCatName = True
#shows the percentage
pie.dataLabels = DataLabelList()
pie.dataLabels.showPercent = True
pie.dataLabels.showCatName = True

pie.legend = None